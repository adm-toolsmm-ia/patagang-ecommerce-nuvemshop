# Dependências
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos de configuração (credenciais em .env — ver ftp-deploy/.env.example)
# config.js agora é seguro — credenciais lidas de process.env (gitignored)
*.env
.env.local
.env.*.local

# Cache e arquivos temporários
ftp-deploy/.deploy-cache.json
*.log
logs/
.cache/

# Backups (podem ser grandes) — rotina usa apenas backups/ftp-full e backups/incremental
backups/
backups/version-01-official/
backup-theme-old/
version-02-prototipo/
*.bak

# Arquivos do sistema
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Arquivos temporários
*.tmp
*.temp
nul

# Environment & Secrets (AIOX)
.env
*.key
*.pem

# Dependencies (AIOX)
node_modules

# Build & Logs (AIOX)
dist/
build/

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
